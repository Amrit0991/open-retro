// Convenience re-export of shared protocol types for client-side modules.
export type {
  TemplateId,
  ColumnDef,
  Card,
  BoardSnapshot,
  ClientMessage,
  ServerMessage,
  ActionResult,
  Identity,
} from '../shared/protocol';
